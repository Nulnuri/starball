# 이어서 작업하기

마지막 정리: **2026-09-04 15:20 KST** (외장하드 분리 전)

## 먼저 알아둘 것

**외장하드가 없어도 아무것도 잃지 않는다.** 전부 깃헙에 있다.

```bash
git clone https://github.com/Nulnuri/starball.git
cd starball && pip install -r requirements.txt
```

드라이브에만 있는 것은 `.cache/` (네이버 응답 캐시) 하나뿐이고, 없으면
다시 받는다 — 느려질 뿐 결과는 같다.

깃헙 계정은 **개인 계정 `Nulnuri`** 다. 회사 계정(`nrchoi-lala`)으로
밀면 404 가 난다. `gh auth switch --user Nulnuri && gh auth setup-git`.

## 지금 상태

| | |
|---|---|
| 커밋 | 전부 완료, 원격과 동일 (`f875088`) |
| 시험 | 66/66 통과 (푸시 22 · 예측 30 · 학습 14) |
| 실주소 | https://starball-9oj.pages.dev |
| 자동 실행 | 매일 10:00 아침 예측, 경기 2시간 전 마감 알림 |
| 구독자 | 1명 (본인) |

## 다음에 확인할 것 — 순서대로

### 1. 알림 두 통이 나란히 남았는지  ← 가장 먼저

2026-09-04 16:30 에 마감 알림이 나갔다. 고친 것이 먹었는지는 **폰 알림창을
보면 안다.**

- 제목에 `9월 4일` 처럼 날짜가 있는가
- 어제 알림과 **두 통이 따로** 남았는가 (한 통만 있으면 아직 덮어쓰는 것)

안 됐으면 `push_send.py` 의 `tag` 와 `web/sw.js:81` 부터 본다.

### 2. 자동 재학습 — 아직 없다

지금은 손으로 `python train_outcome.py --emit` 을 돌려야 모델이 갱신된다.
월 1회 자동으로 돌리고, **검증 점수가 나빠지면 갱신을 거부하는 장치**를
같이 넣어야 한다. 그게 없으면 조용히 나빠지는 모델을 배포하게 된다.

### 3. 하드코딩으로 남은 두 값

`outcome_infer.py` 의 `HOME_EDGE = 0.035`, `DRAW_SHARE = 0.02` 는 손으로
박은 값이다. 기록에서 재서 넣어야 한다. 구장 팩터를 그렇게 바꿨더니
2025 대전에서 0.861 → 1.07 만큼 틀려 있었다.

### 4. 2027 개막 — 신규 잠실야구장

구장 팩터는 기록에서 계산하므로 새 구장도 자동으로 잡힌다. 다만 개막 직후
몇 주는 표본이 없어 팩터가 1.0(중립)에 붙어 있다. 그동안 화면에
**"구장 데이터 부족"** 을 띄워야 한다. 지금은 아무 표시가 없어서 신뢰할 수
있는 숫자처럼 보인다.

### 5. 비시즌 크론 정지 (11월~2월)

깃헙은 저장소가 **60일 동안 아무 커밋이 없으면 크론을 끈다.** 시즌이
끝나면 커밋이 멈추니 3월 개막 전에 다시 켜야 한다. 2월 말에 아무 커밋이나
하나 밀어두면 된다.

## 기대치는 이미 못박아 뒀다

`HANDOFF.md` 맨 앞 **"이 도구가 실제로 할 수 있는 것"** 을 먼저 읽을 것.
요약하면:

- 이 도구 7.16% / 고정값 6.44% — 3시즌 419경기에서 **3경기** 차이다
- 144경기 기대 스타볼 10.3개 (고정값 9.3개)
- **무작위는 0.8개.** 하지 말 것
- 천장은 야구의 성질이다 — 득실 차 1점 23.5% 를 어떤 방법으로도 못 넘는다

적중률을 올리라는 요구가 다시 오면, **먼저 이 숫자를 보여주고** 무엇이
가능한지 합의한 다음 시작할 것. 그러지 않아서 한 번 크게 돌아갔다.

## 자주 쓰는 명령

```bash
python starball_predictor.py            # 오늘 예측
python build_gamelog.py                 # 경기 기록 갱신 (증분)
python train_outcome.py --emit          # 승패 모델 재학습
python train_hr.py --emit               # 홈런 모델 재학습
python test_starball.py && python test_push.py && python test_train.py

gh workflow run starball.yml -f notify=false   # 배포만, 알림 없이
gh workflow run starball.yml -f notify=true    # 알림까지 실제 발송
```

**`notify=true` 를 함부로 쓰지 말 것.** 2026-09-04 에 확인하려고 손으로
돌리다 같은 알림을 하루에 다섯 번 보냈다. 기본값이 false 인 이유다.
