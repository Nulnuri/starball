# 네이버 API 응답 캐시 — 다시 받으면 되고, 오래되면 오히려 방해가 된다
.cache/
__pycache__/
*.pyc

# 화면 시안 원본(.dc.html)은 남기고, 편집기가 들어간 산출물만 뺀다.
# 그 파일은 2.4MB 인데 내용은 원본에서 다시 만들 수 있다.
design/starball-app-screens.html

# 전달용 묶음 (필요할 때 다시 만든다)
*.zip

# 문서는 web/docs/ 에 있다. 뿌리의 사본은 중복이라 넣지 않는다
스타볼-*.html

# 임시
_*.png
*.log

# wrangler 로컬 실행이 남기는 빌드 부스러기. 저장소에 들어가면 안 된다.
.wrangler/

# 로컬 검증용 임시 파일
_*.log
_chk.js
_t/
