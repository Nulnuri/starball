# 네이버 API 응답 캐시 — 다시 받으면 되고, 오래되면 오히려 방해가 된다
.cache/
__pycache__/
*.pyc

# 화면 시안. 편집기 페이로드가 2MB 넘어 저장소에 둘 이유가 없다
design/

# 전달용 묶음 (필요할 때 다시 만든다)
*.zip

# 문서는 web/docs/ 에 있다. 뿌리의 사본은 중복이라 넣지 않는다
스타볼-*.html

# 임시
_*.png
*.log
